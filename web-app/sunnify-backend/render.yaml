services:
  - type: web
    name: sunnify-backend
    env: docker
    region: oregon
    plan: free
    dockerContext: ../..
    dockerFilePath: Dockerfile
    healthCheckPath: /api/health
    autoDeploy: true
